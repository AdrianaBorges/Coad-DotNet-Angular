import { AppToogleDirective } from './app-toogle.directive';

describe('AppToogleDirective', () => {
  it('should create an instance', () => {
    const directive = new AppToogleDirective();
    expect(directive).toBeTruthy();
  });
});
