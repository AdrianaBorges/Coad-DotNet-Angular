import { MaskDinheiroDirective } from './mask-dinheiro.directive';

describe('ShowDinheiroDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskDinheiroDirective();
    expect(directive).toBeTruthy();
  });
});
